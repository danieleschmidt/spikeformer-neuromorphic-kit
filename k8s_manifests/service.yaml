apiVersion: v1
kind: Service
metadata:
  name: spikeformer-service
  namespace: spikeformer-prod
  labels:
    app: spikeformer
spec:
  selector:
    app: spikeformer
  ports:
  - port: 80
    targetPort: 8080
    name: http
  type: ClusterIP
---